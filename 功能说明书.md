# Artco 功能说明书

> 一款专注于截图标注与 AI 分析的桌面工具

---

## 一、快速入门

- **启动方式**：运行 `Artco.exe`，系统托盘会出现 Artco 图标，桌面出现胶囊浮窗
- **截图快捷键**：`F1`（可在设置中自定义）
- **退出方式**：右键托盘图标 → 退出

---

## 二、核心功能

### 1. 截图 & 快速标注

按下 `F1` 进入截图模式：

| 操作 | 说明 |
|------|------|
| **框选区域** | 鼠标拖拽选择截图范围 |
| **调整选区** | 拖动边缘/角点调整大小，选区内拖动移动位置 |
| **快速标注** | 使用工具栏的涂鸦(`D`)、文字(`T`)、矩形(`R`)等工具 |
| **完成截图** | `Enter` 复制到剪贴板，`Ctrl+S` 保存文件 |
| **贴图** | `P` 将截图固定在屏幕上 |
| **进入编辑器** | `E` 打开高级编辑器 |
| **OCR 识别** | 工具栏 OCR 按钮，识别选区内文字 |

**选区微调快捷键**：
- 方向键：像素级移动选区
- `Shift + 方向键`：扩展/收缩选区边缘

---

### 2. 编辑器（高级标注）

从截图界面按 `E` 进入，提供完整的标注工具：

| 工具 | 快捷键 | 说明 |
|------|--------|------|
| 选择 | `V` | 选中/移动已有标记 |
| 序号点 | - | 自动编号的圆形标记，适合步骤说明 |
| 矩形框 | `R` | 高亮某个区域 |
| 箭头 | - | 指向性标注 |
| 画笔 | `D` | 自由涂鸦 |
| 文字 | `T` | 添加文字说明 |
| 橡皮擦 | - | 删除标记 |

**编辑器操作**：
- `Ctrl+Z`：撤销
- `Ctrl+Y`：重做
- `Ctrl+A`：全选
- `Ctrl+C`：复制图片
- `Ctrl+S`：保存图片
- 鼠标滚轮：缩放画布
- 中键拖动：平移画布

---

### 3. 贴图功能

截图后按 `P`，将截图固定在屏幕最前端：

- 可拖动位置
- 透明度可调
- 适合对照参考
- **双击缩小**：双击贴图可将其缩小为缩略图（参考 Setuna）
- **裁剪缩小**：在设置中开启"裁剪缩小"后，双击时将以鼠标点击位置为中心裁剪周围区域作为缩略图

**缩略图设置**（设置 → 通用）：
- 开启/关闭裁剪缩小功能
- 设置缩略图大小（32/48/64/96/128 像素）

---

### 4. AI 图片分析

在截图工具栏或编辑器中点击 **AI** 按钮：

1. 选择预设的 Prompt（如"转 MJ 提示词"）
2. 或输入自定义指令
3. AI 分析结果会以气泡形式展示

**Prompt 管理**：通过设置可添加/编辑自定义 Prompt 模板，支持"文本分析"和"图片生成"两种模式。

---

### 5. OCR 文字识别

在截图工具栏点击 **OCR** 按钮：

- 使用 Windows WinRT OCR 引擎识别选区内文字
- 默认中文简体，不支持时自动回退英文
- 识别结果在弹窗中展示，可编辑和复制

---

### 6. 归档管理

点击胶囊浮窗的归档按钮（或双击胶囊），查看历史记录：

- **归档**：手动保存的截图 + 标注 + AI 结果
- **剪贴板历史**：自动记录复制过的图片（应用运行期间）

支持操作：
- 重新 AI 分析（支持文本/图片生成模式切换）
- 复制 / 删除
- 拖拽图片到外部应用

---

## 三、快捷键速查

### 全局
| 快捷键 | 功能 |
|--------|------|
| `F1` | 启动截图 |

### 截图模式
| 快捷键 | 功能 |
|--------|------|
| `Enter` | 确认并复制 |
| `Esc` | 取消截图 |
| `Ctrl+C` | 复制 |
| `Ctrl+S` | 保存 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` | 重做 |
| `Ctrl+A` | 全选 |
| `P` | 贴图 |
| `E` | 进入编辑器 |
| `R` | 矩形框工具 |
| `D` | 涂鸦工具 |
| `T` | 文字工具 |
| `V` | 取消工具（选择模式） |
| `Tab` | 显示/隐藏工具栏 |

---

## 四、设置项

右键托盘图标 → 设置：

- **快捷键配置**：自定义截图快捷键
- **AI 配置**：设置 Gemini API Key
- **Prompt 管理**：编辑 AI 分析模板（文本分析/图片生成）

---

## 五、系统要求

- Windows 10/11
- 屏幕分辨率 1920×1080 及以上（推荐）
