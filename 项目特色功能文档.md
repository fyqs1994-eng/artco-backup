# Artco 项目特色功能文档

> 本文档仅聚焦 Artco 的特色能力（差异化功能），不展开常规截图、基础标注、基础保存等通用能力。

---

## 1. 视觉 AI 的多轮追问链路（不是一次性问答）

### 特色说明
- 首次基于截图发起 AI 分析后，结果气泡可直接进入“追问”模式，形成连续对话。
- 对话上下文会被保留，后续问题可沿用同一任务语境，而不是每次重新开始。

### 差异化价值
- 降低“重新描述问题”的成本，适合复杂图像解释、UI 评审、代码截图逐步拆解。
- 支持“先粗分析，再精细追问”的工作流，提升实际可用性。

### 典型场景
- 设计评审：先让 AI 总览页面，再追问某个组件交互是否合理。
- 代码截图分析：先提取逻辑，再追问潜在 bug 与优化点。

---

## 2. 屏幕贴图的“裁剪缩小”模式（Setuna 风格）

### 特色说明
- 贴图双击时可进入缩略图模式，并支持以鼠标落点为中心的自动裁剪缩小。
- 缩略图尺寸可配置（如 32/48/64/96/128），适配不同屏幕与工作习惯。

### 差异化价值
- 相比仅“整体缩小”，裁剪缩小能保留用户最关心的局部细节。
- 在多窗口协作中，既不遮挡主工作区，又能持续参考关键区域。

### 典型场景
- 开发联调时，将接口字段、色值、关键按钮状态固定在屏幕边缘对照。
- 设计还原时，把参考图重点区域缩成小窗常驻。

---

## 3. 鼠标侧键触发的剪贴板悬浮画廊

### 特色说明
- 可将鼠标侧键（X1/X2）绑定为“剪贴板悬浮面板”唤起键，在鼠标位置直接弹出历史内容网格。
- 支持卡片单击快速粘贴与拖拽投递到目标应用（含浏览器上传场景兼容）。
- 对大图拖拽做了临时文件和尺寸策略处理，减少拖拽卡顿或失败。

### 差异化价值
- 将“复制-切窗口-粘贴”的线性流程，压缩成就地选取与投递。
- 对高频素材搬运用户（设计、运营、测试）有明显效率优势。

### 典型场景
- 从历史截图中快速拖入 IM、文档、浏览器上传框。
- 在多个工具之间反复切换时，降低粘贴步骤与误操作。

---

## 4. 归档 + 剪贴板双轨历史系统

### 特色说明
- 同时维护“手动归档记录”和“运行期剪贴板历史”两套数据视图。
- 归档详情支持继续发起 AI（文本分析/图片生成切换）、复制、删除、拖拽等再利用操作。

### 差异化价值
- 不仅“存截图”，而是把截图、AI 结果、后续操作串成可复用资产。
- 让临时灵感和正式成果都可追溯，减少内容丢失。

### 典型场景
- 方案讨论后回查某次 AI 解释过程，并继续追加分析。
- 从历史记录直接回拖素材到新任务中，避免重复截图。

---

## 5. Prompt 工程内置化（模板化 + 类型化）

### 特色说明
- 提供可视化 Prompt 管理：新增、编辑、删除、默认模板设置。
- Prompt 区分“文本/识图（Vision）”与“生图/重绘（Image Gen）”类型，减少误用。

### 差异化价值
- 把个人 prompt 经验沉淀为团队可复用模板。
- 同一工具可覆盖“分析”和“生成”两类任务，不必在外部平台来回切换。

### 典型场景
- 团队统一“UI 评审模板”“代码审查模板”“生图风格模板”。
- 不同岗位按模板一键套用，提高输出一致性。

---

## 6. 多服务商 AI 接入与可切换配置

### 特色说明
- 支持 Google / OpenAI / Anthropic 等服务商与多模型配置。
- 提供服务商启用开关、当前服务商选择、API Key 与 Base URL 管理能力。

### 差异化价值
- 便于在成本、速度、效果之间做动态平衡。
- 更适合企业或团队在不同网络/网关环境下接入。

### 典型场景
- 日常任务用低成本快速模型，关键任务切换高质量模型。
- 在代理网关或私有转发环境下保持统一工具体验。

---

## 7. 工作区“分配面板”：截图即资产化落盘

### 特色说明
- 可将截图按“工作区 / 主题 / 版本 / 资源类型 / 制作进度”进行结构化分配。
- 分配前实时预览目标文件路径，避免命名混乱和目录失控。

### 差异化价值
- 让截图天然进入项目资源管理体系，而不是散落在桌面或下载目录。
- 对长期项目尤其友好，便于多人协同和后续检索。

### 典型场景
- 设计资产按主题与版本归档，快速定位某轮方案。
- 测试缺陷截图按进度归类，形成可追踪证据链。

---

## 8. Windows 资源管理器缩略图格式标识扩展（桌面生态延展）

### 特色说明
- 集成 Shell 扩展后，资源管理器缩略图可叠加格式标签（PSD/PNG/JPG/RAW 等）。
- 对 PSD/PSB 文件提供专门处理逻辑，提升素材目录可读性。

### 差异化价值
- 将“格式识别”前置到浏览层，减少误开文件和筛选成本。
- 对图像资产密集场景（设计库、素材库）有明显提效。

### 典型场景
- 海量图片目录快速识别源文件格式与可编辑性。
- 在不打开 Photoshop 的情况下先做第一轮筛选。

---

## 9. 特色能力组合建议（推荐对外表述）

- **智能截图中枢**：截图 -> AI 理解 -> 多轮追问 -> 归档复用，一条链路闭环。
- **轻量资产流转**：侧键唤起历史 -> 拖拽投递 -> 工作区分配，减少素材搬运阻力。
- **桌面级常驻效率**：贴图裁剪缩小 + 悬浮面板 + 托盘工作流，强调“不中断主任务”。

---

## 10. 非本文档范围（刻意省略）

以下属于常规能力，本文不作为重点：
- 基础截图、基础标注、基础复制/保存
- 常规快捷键设置与普通界面交互
- 一般性的窗口样式与主题表现

