# Artco 项目特色功能文档（简版）

> 仅保留差异化能力，不展开常规截图/标注功能。

## 1) AI 多轮追问（连续对话）
- 截图分析后可直接“追问”，保留上下文，不用重复上传和重复描述。
- 适合复杂 UI、代码截图、需求评审的逐步深挖。

## 2) 贴图裁剪缩小（Setuna 风格）
- 双击贴图可进入缩略图模式，并支持“按鼠标落点自动裁剪缩小”。
- 重点区域可常驻屏幕边缘，减少遮挡同时保留关键信息。

## 3) 侧键唤起剪贴板悬浮画廊
- 鼠标侧键可一键弹出历史内容网格，就地选择并拖拽到目标应用。
- 对浏览器上传和大图拖拽做了兼容优化，降低失败率与卡顿。

## 4) 归档 + 剪贴板双轨历史
- 同时保存“正式归档”和“运行期剪贴板历史”。
- 历史记录可继续 AI 分析、复制、删除、拖拽，实现二次利用。

## 5) Prompt 模板化 + 类型化
- 内置 Prompt 管理（新增/编辑/默认模板），支持文本分析与生图两类模板。
- 让个人经验沉淀为可复用规范，提升团队输出一致性。

## 6) 多服务商 AI 可切换
- 支持多模型与多服务商配置（如 Google/OpenAI/Anthropic）。
- 可按任务在速度、成本、质量之间灵活切换。

## 7) 工作区分配面板（截图即资产化）
- 截图可按主题/版本/类型/进度结构化落盘，并预览目标路径。
- 让素材可追溯、可检索，避免文件散落。

## 8) Windows 缩略图格式标识扩展
- 在资源管理器缩略图上显示格式标签（PSD/PNG/JPG/RAW 等）。
- 海量素材筛选更快，减少误开文件。

---

**一句话定位**：Artco 不只是截图工具，而是“截图 -> AI 理解 -> 历史复用 -> 资产管理”的桌面效率闭环。

